# 「观息」视觉疲劳引擎 & 专注模式 v2 —— 自研算法设计文档

> 版本：v4.5.0 · 引擎实现：`fatigue-engine.js`（729 行）· 渲染层：`content.js`
> 专注后台：`background.js` 专注模块 · 状态 UI：`modules/focus.js`
> 所有计算均在本地完成，不采集、不上传任何数据。

---

## 一、视觉疲劳引擎（EyeCare Engine）

### 1.1 分层架构

```
L1 信号层   S1 键入强度  S2 点击强度  S3 滚动(速度+路径熵)  S4 鼠标运动学
            S5 内容卷入(视频/阅读)  S6 时长与节律
L2 归一化层 Welford 在线均值/方差 → z-score；P² 在线分位数 → 个人基线
L3 模型层   M1 任务疲劳 logistic 曲线  M2 昼夜节律  M3 指数恢复  M4 几何融合
L4 个性化层 fatigueProfile 持久化（10 分钟节流写盘）
L5 输出层   双 EMA 平滑 + 趋势项 → 迟滞分级 1-5 + 置信度
```

### 1.2 信号定义（L1）

| 信号 | 特征 | 疲劳方向 |
|------|------|----------|
| S1 键入 | 事件率（分钟 EMA）、节奏变异系数 | 速率异常升高（硬撑）/ 节奏"一顿一顿" |
| S2 点击 | 事件率（分钟 EMA） | 速率异常升高 |
| S3 滚动 | 速度分布 μ、方向序列熵 | 速度下降 + 熵升高（漫无目的来回找） |
| S4 鼠标 | 速度 μ、方向反转率、急动度直方图熵 | 速度低于个人基线、微颤动多、动作僵硬 |
| S5 内容 | 视频连续观看占比、滚动深度增速衰减 | 被动用眼、阅读速度下降 |
| S6 状态 | 活跃占比 | "游魂"（在页面但长时间无交互） |

**路径熵**：把滚动方向序列切 24 段，统计每段净方向并分桶，计算
Shannon 熵 `H = -Σ p·log p / log 24`。专注阅读的熵低（单向），疲劳"来回找"的熵高。

**急动度熵**：鼠标 |Δv|/dt 分 8 桶直方图取 Shannon 熵。疲劳时动作僵硬单调，熵下降。

### 1.3 归一化（L2）

**Welford 在线统计**（数值稳定单遍扫描）：

```
n ← n+1
δ ← x − mean
mean ← mean + δ/n
M2 ← M2 + δ·(x − mean)
variance = M2/(n−1)
```

**z-score → 疲劳贡献**（logistic 压缩）：

```
s(z) = σ(z; k=1.6, x₀=0.8) = 1 / (1 + e^(−1.6·(z−0.8)))
```

含义：特征高于**个人常态**约 0.8σ 开始显著贡献疲劳。样本 < 12 时返回 null（冷启动不误判）。

**P² 分位数估计**（Jain & Chlamtac 1985）：O(1) 内存维护 p35（鼠标速度"慢"基线）
与 p70（反转率"颤动"基线），5 个标记点 + 抛物线预测调整。意义：
阈值不是全局常数，而是**相对用户自己的历史分布**——高频操作者与慢节奏阅读者的基线自动不同。

### 1.4 模型（L3）

**M1 任务疲劳 logistic 增长**（等效高强度分钟 t）：

```
F(t) = 1 / (1 + e^(−k·(t − t₀)))
t₀ = lerp(45, 25, intensity)   强度越大，中点从 45min 提前到 25min
k  = lerp(0.045, 0.075, intensity)
```

**M2 昼夜节律系数**（双谷模型）：

```
C(h) = 1 − 0.12·cos(2π(h−15)/24)        15 点最清醒、凌晨 3 点最困
     + 0.10·(1−|h−3.5|/1.5)   h∈[2,5)    主谷加深
     + 0.04                  h∈[14,16)  午后次谷
C ∈ [0.85, 1.18]，乘法作用于节律子分数
```

**M3 指数恢复**（空闲期衰减）：

```
τ = 1.5 min（短歇）→ >5 min 长歇 τ = 25/60 min（恢复更快）
疲劳势 reserve ← reserve · 0.5^(idleMin/τ)
>20 min 额外 ×0.35
```

**M4 几何加权融合**：

```
score01 = Π sᵢ^{wᵢ}   （权重按信号可用性归一）
补偿项：max(sᵢ) > 0.75 时整体上抬 (max−0.75)·0.5
```

几何平均对"单信号爆表"的惩罚强于算术平均（0.9,0.9,0.1 的算术均值 0.63，几何 0.42），
避免"其他都正常、单一信号异常"被稀释。

最终相对权重：行为融合 55% + 任务曲线 30% + 节律 15%。

### 1.5 输出（L5）

- **双 EMA**：快 α=0.35（跟手）、慢 α=0.12（稳定），趋势 trend = fast − slow
- **迟滞分级**（阈值 15/35/55/75）：
  - 升级：越过阈值且**持续 30s**（防抖动）
  - 降级：即时，但 5→4 需 trend ≤ 6（还在上升就不降）
- **置信度** = 0.25 + 可用信号数×0.09 + min(鼠标样本,400)/400×0.25

### 1.6 个性化档案（L4）

`chrome.storage.local.fatigueProfile`：7 个 Welford + 2 个 P² 分位器 + 会话计数。
每 60s 学习推入、每 10 分钟节流写盘。设置里可 `resetCalibration()` 重新校准。

---

## 二、专注模式 v2 算法

### 2.1 注意力状态机（每次黑名单访问独立评估）

```
命中黑名单站点 → dwell（评估中，since=t₀）
  ├─ 离开且停留 < 45s → resisted（忍住）✅ 通知鼓励
  └─ 离开且停留 ≥ 45s → broken（破戒）❌
```

### 2.2 自适应提醒（对抗习惯化）

```
interval(n) = min(90s · 1.35ⁿ, 300s)     同一 host 每提醒一次 n+1
文案池 6 条按 n 轮换                         防「提醒看腻失效」
```

### 2.3 威胁评分（个性化建议黑名单）

```
threat(d) = 0.5·visits(d)/maxVisits + 0.3·min(temptations(d)/6, 1) + 0.2·recency(d)
visits     近 30 天 chrome.history 访问量归一
temptations 专注期该域名被提醒/犯戒次数
recency    1 − (now − lastVisit)/30d
threat ≥ 0.35 进入 Top12 建议
```

### 2.4 番茄周期与报告

- 自然到期的会话：自动排 5 分钟短休，**每 4 轮 15 分钟长休**（`hc-focus-break` 闹钟）
- **专注效率** `= 1 − 0.15·broken − 0.03·nudges`（下限 0）
- **忍住率**：resisted / (resisted + broken)
- 报告历史保留 30 条；`suggestNextMinutes()` 按近 5 次完成率自适应建议下次时长
  （完成率 ≥80% 加 10 分钟，≤40% 减 10 分钟，5 分钟取整）

---

## 三、代码量与边界说明

| 组件 | 行数 | 性质 |
|------|------|------|
| fatigue-engine.js | 729 | 自研引擎（全部真实可运行） |
| content.js（渲染层，重构后） | 215 | 渲染 + 上报 |
| focus 后台（background.js 专注模块） | ~250 | 状态机/威胁/番茄 |
| modules/focus.js | 236 | 专注 UI |

**诚实声明**：本仓库两个功能的真实算法代码合计约 1400 行（另有设计文档），不到一万行。
这是"把每个函数都做有意义"前提下的真实规模；任何为凑到一万行而添加的部分都将是注水，
不建议在评审场合声称超出实际规模的数字。

---

## 四、每日增量路线图（每天推进一项，做完打勾）

> 约定：每项都必须是**真实可运行的算法/功能**，禁止注水。
> 完成流程：实现 → `node --check` 全部触及文件 → `build_all.py` 打包 → 同步桌面/GitHub → 在此处打勾并记录。

- [x] **Day 1（已完成 v4.6.0）**
  - 护眼：趋势预测 `predict()`（双 EMA 斜率启发式 → 「距下一级还剩 N 分钟」，置信度低输出 null）
  - 护眼：20-20-20 微休息教练（连续活跃 20 分钟且 ≥2 级 → 提示远眺 20 秒）
  - 专注：每日目标（默认 60 分钟，只计完成会话；进度条 + 达成通知）
  - 专注：周报聚合（近 7 天总量 / 完成率 / 平均效率 / 忍住率）
- [ ] **Day 2**：护眼——内容类型自适应权重（code/article/table/generic 四类页面用不同信号权重与分级阈值；`detectType()` 结果接入引擎权重表）
- [ ] **Day 3**：专注——白名单模式（会话可选「只许列表内站点」，反向判定 dwell 逻辑）
- [ ] **Day 4**：护眼——周级疲劳画像（按日聚合疲劳曲线，输出每日常态 μ/σ 与离群日标记，弹窗展示小趋势）
- [ ] **Day 5**：专注——提醒即速记（通知带按钮？MV3 限制 → 改为扩展弹窗内「分心原因」快选，写入 focusEvents，供威胁评分加权）
- [ ] **Day 6**：护眼——引擎自诊断（输出各信号健康度：样本量/方差塌缩检测/基线漂移告警，写入 fatigueProfile.diagnostics）
- [ ] **Day 7**：专注——威胁评分二阶化（引入「时段敏感性」：同一域名在深夜 vs 工作时段威胁分不同）
- [ ] **Day 8**：护眼——爆表信号源追溯（breakdown 里定位本次主要贡献信号，建议针对性休息动作）
- [ ] **Day 9**：专注——会话回放（按 focusEvents 时间轴渲染单次会话的抵抗/破戒序列图，纯 CSS 条带）
- [ ] **Day 10**：护眼+专注——健康分接入（健康分新增「专注纪律」分项：忍住率与目标完成率加权）
- [ ] **Backlog**：引擎——马尔可夫转移矩阵建模等级跳变；专注——强化学习式提醒时机优化（用忍住率做 reward）
