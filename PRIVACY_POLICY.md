# 隐私政策（Privacy Policy）

**适用扩展**：浏览器搭子 · 历史与标签页管家（Browser Companion · History & Tab Manager）
**版本**：4.2
**生效日期**：2026 年 8 月 31 日
**开发者 / 联系方式**：本项目为开源扩展，代码托管于 GitHub 仓库 `yoyo636/chromium-history-cleaner`（https://github.com/yoyo636/chromium-history-cleaner ）。如对本政策有任何疑问，请通过该仓库的 Issues 与我们联系。

---

## 一、重要声明（TL;DR）

**本扩展不在任何服务器上收集、存储、传输或出售任何用户数据。所有数据处理均在您的本地设备（浏览器）内完成。** 扩展不含任何分析 SDK、广告 SDK、追踪器或第三方代码，也不加载任何外部 CDN 资源。

---

## 二、我们收集与处理的数据

本扩展仅在**您主动操作对应功能时**，通过浏览器官方 API 读取本地数据，用于实现您所请求的功能。具体如下：

| 数据类别 | 用途 | 是否离开设备 |
| --- | --- | --- |
| 浏览历史（访问时间 / 标题 / URL） | 「历史」模块的查询、预览、过滤、按时间段删除、导出备份；「统计」模块的访问分析 | 否 |
| 标签页信息（标题 / URL / 窗口） | 「标签」模块的批量关闭、去重、按域名关闭、复制网址、存档会话 | 否 |
| 书签数据 | 「书签」模块的去重、死链检测、导出（JSON / HTML）、删除 | 否 |
| 下载记录（文件名 / 来源 URL / 文件大小） | 「下载」模块的查看、打开、打开文件夹、移除记录；「清理」模块显示真实占用 | 否 |
| 浏览数据（缓存 / Cookie / 历史 / 表单 / 密码等） | 仅在您点击「清理」并确认后，通过系统级 API 清除；**本扩展不读取、不保存 Cookie 或密码的内容** | 否 |
| 会话存档（URL 列表） | 「会话」模块的自定义会话保存 / 恢复 / 删除，存于扩展本地存储 | 否 |
| 网页交互信号（鼠标移动 / 滚动 / 键盘节奏 / 页面长任务 / 帧率） | 「护眼」与「性能」模块在本地实时计算疲劳等级与资源占用，用于渐进式阅读调整与高负载提示 | 否 |
| 指纹调用事件（页面是否读取 Canvas / WebGL 等指纹 API） | 「隐私」模块生成本地隐私报告，帮助您识别潜在追踪行为；「一键加固」仅在本地对指纹输出随机化 | 否 |
| 偏好设置（主题 / 默认时间范围 / 开关状态） | 保存您的个性化设置，存于扩展本地存储 | 否 |

**我们明确不收集**：您的姓名、邮箱、账号信息、密码、支付信息、真实身份信息，以及任何可定位到个人的身份标识。

---

## 三、无痕（隐身）模式说明

在无痕模式窗口中，本扩展遵循浏览器的无痕语义：
- 无痕模式下产生的记录（如隐私指纹报告、疲劳数据）同样仅存于本地，并在浏览器会话结束后由浏览器按无痕规则处理。
- 「隐私」模块会在报告中标注某次指纹调用是否发生在无痕模式下，帮助您了解哪些网站试图在无痕模式下识别您的设备。
- 注意：无痕模式本身无法阻止网站通过指纹技术识别设备；本扩展的「一键加固」可在本地随机化 Canvas / WebGL 指纹以降低被识别风险，该处理不涉及任何数据传输。

---

## 四、数据的存储与保留

- 扩展产生的数据（偏好、会话存档、当日疲劳曲线、隐私事件记录、性能快照）保存在浏览器提供的 `chrome.storage.local` 本地存储中，**不会上传到任何远程服务器**。
- 浏览器内置数据（历史、书签、下载记录等）由浏览器自身管理，本扩展仅按您的指令查询或删除。
- 您可以随时：
  - 在「隐私」页点击「清空记录」删除本地隐私事件；
  - 在「会话」页删除已存档会话；
  - 通过**卸载扩展**删除扩展产生的全部本地数据（卸载后 `chrome.storage.local` 数据随扩展一并移除）。
  - 扩展不维护任何服务端数据副本，故不存在需要您单独申请删除的服务端数据。

---

## 五、数据共享与第三方

- **我们不会**向任何第三方出售、出租或共享您的数据；本扩展没有服务端，开发者无法接触您的任何数据。
- 扩展不包含第三方分析、广告、崩溃上报或统计 SDK；代码全部内嵌于扩展包内，不加载外部脚本或 CDN。
- **插件自身发起的网络请求**仅有一类且由浏览器执行：书签「死链检测」对书签 URL 发起 `HEAD` 探测（仅传输该 URL 本身，用于判断链接是否失效）。除此之外插件不发起任何网络传输。

---

## 六、权限说明

本扩展声明以下权限，均用于实现上述功能，遵循最小权限原则：

| 权限 | 用途 |
| --- | --- |
| `history` | 历史查询、按时间段 / 按 URL 删除、导出 |
| `tabs` / `sessions` | 标签管理、最近关闭恢复、会话存档 |
| `bookmarks` | 书签去重、死链检测、导出、删除 |
| `downloads` | 下载记录管理、导出文件落盘 |
| `browsingData` | 按您的确认清除浏览数据（缓存 / Cookie 等） |
| `tabCapture` | 「音频」模块在您点击「分析」后获取当前标签音频流做频谱分类（需用户触发） |
| `host_permissions`（http/https） | 「隐私」模块注入指纹监控与随机化脚本；护眼 / 性能 / 广告拦截等内容脚本在网页运行 |
| `cookies` | 「清理」模块统计各站点 Cookie 数量与占用（仅计数与名称，不读取 Cookie 值） |
| `alarms` | 护眼休息提醒、性能持续高负载预警的定时触发（替代持久后台计时） |
| `notifications` | 高负载与自动静音提醒 |
| `storage` | 保存偏好、会话、疲劳 / 隐私 / 性能本地数据 |

---

## 七、儿童隐私

本扩展不面向 13 岁以下儿童设计，也不故意收集任何儿童的个人信息。

---

## 八、政策变更

如本隐私政策发生变更，我们将在扩展商店页面与本页面同步更新并标注新的生效日期。重大变更将通过商店页面公告。

---

## 九、联系我们

如对本隐私政策或扩展的数据处理有任何疑问，请通过 GitHub 仓库 Issues 联系我们：https://github.com/yoyo636/chromium-history-cleaner/issues

（开发者可在此处替换为自己的联系邮箱。）

---

*本隐私政策最后更新：2026 年 8 月 31 日。*
